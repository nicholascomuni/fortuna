# Local dev points at the Flask dev server; see .env
# In Cloudflare Pages, set VITE_API_URL as a project environment variable
# (Settings > Environment variables) pointing at the App Runner service URL, e.g.:
VITE_API_URL=https://<app-runner-service-url>/api
